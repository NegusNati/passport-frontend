# Local ad-dev compose runs the API over plain HTTP on AD_DEV_API_PORT.
# This file intentionally overrides the backend image's production SSL vhost,
# which expects Cloudflare origin certificate files.
